#Download Python
https://www.python.org/downloads/

#Criar venv, (o nome da pasta criada será "venv")
python -m venv venv

#Ativar venv
na pasta c:\NewProject\venv\Scripts
digite o comando "activate"
ou ". activate"

//Máquina virtual ativada
"(venv) c:\NewProject\venv\Scripts>"

#Download Django
https://www.djangoproject.com/download/
pip install --pre django
pip install Django==3

#Criar projeto Django ("blog" será o nome do projeto)
django-admin startproject blog

//Criar projeto Django sem subpasta
django-admin.py startproject front .

#start projeto django
python manage.py runserver

#Download Node
https://nodejs.org/en/download/

#Download Angular
https://cli.angular.io/
npm install -g @angular/cli

#Criar Banco de Dados
"(venv) c:\NewProject\project-backend>"
python manage.py migrate

#Criar usuário administrador
python manage.py createsuperuser

usuário: Administrador
senha: DWbi2049

#Download Django Rest Framework
pip install djangorestframework

#Criar API
python manage.py startapp core

#Instalar pacote para suporte de imagens
pip install Pillow

#Criar arquivo de migração
python manage.py makemigrations
python manage.py migrate

#Gerar Service
ng generate service api

#Instalar CORS para permitir acesso Http no Django
pip install django-cors-headers

#Gerar novo componente
ng generate component members-detail

#Tornar Git Repositório
git init

#Criar arquivo que ignora pastas no Git Repositório
vim .gitignore
"i" para inserção das pastas
"esc" para sair
":wq" fechar e salvar

#Adicionar o projeto, caso não funcione troque "." pelo nome da pasta ou arquivo
git add . 

#Criar o commit
git commit -m "first commit"

git remote add origin https://github.com/emersondasilva/django-angular.git

git push -u origin master

#Remover .git
rm -rf .git

#Templates
https://html5up.net/

#Biblioteca JWT
pip install djangorestframework-jwt

#Instalar Arquivo de requisitos do Django
pip install -r requirements.txt

#Instalar driver postgres
pip install psycopg2

#Install
npm install --save jwt-decode
npm install --save @types/jwt-decode
npm install moment